public 클래스
private 데이터(속성 인스턴스변수)
public 생성자 (리턴타입 없음)
public 메소드

패키지명 소문자로만 쓰는게 일반적.대문자를 거의 아예 안쓴다.
회사명은 주소 거꾸로 씀 .
클래스명 반드시 대문자로 시작. 알파벳,숫자,언더바,달라(오라클에서 씀)
생성자는 반드시 public, return type이 없다 
데이터는 초기화하지않으면 객체생성시 자동초기화 

같은패키지거나 java.lang에 있는클래스가 아니라면 무조건 import

매개변수 있는 생성자는 데이터를 초기화 하는게 존재의의 
매개변수있는생성자를 만드려면 디폴트생성자를 만들어야한다. 오버로딩이 필요

하위 디렉토리에 있는 패키지라고 하더라도 같은 패키지가 아니다.
따로 import 해야한다.

참조변수끼리의 할당은 레퍼런스(주소)를 할당한다. 
배열도 참조변수기때문에 마찬가지
같은 주소값을 가지기때문에 같은 객체를 가리킨다.
사실상 같다.

객체 배열
배열형 변수의 주소값을 따라서 배열을 가보면 그 배열에는
각각의 객체 주소를 갖고있는 객체들이 있다.
즉 이중 주소값.

하나의 자바파일에 클래스를 여러개 만들어봤자 하나의 파일로 컴파일되는게
아니고 각각의 클래스파일로 컴파일이 된다.
그러나 그 클래스들은 public도 아니기때문에 다른 패키지에서 사용도 못한다.
결국 쓸모없는 파일이 되어버림.
어지간하면 하나의 자바파일은 하나의 클래스만 구현해야한다.



ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ
this란 객체 자신 

ctrl +클릭 시 해당관련 클래스로 이동한다

source탭에서 getter setter 한번에 생성하기 편하다 

import a.b.c.*;  // * c아래의 모든것을 다 쓰겟다 

ctrl shift O  =  필요한 임포트만 알아서 해줌 

레퍼런스(주소)값을 갖고 있는 변수 레퍼런스변수 = 객체 변수

아스트릭마크 = 별마크

출력만 할때는 확장for문이 훨씬 자주쓰인다 
확장포문 구조  for(타입 변수 : 배열변수){실행}

return abc+"abc"
return String.format("abc %s",abc) 
printf랑 같은 방식의 입력하는 방법