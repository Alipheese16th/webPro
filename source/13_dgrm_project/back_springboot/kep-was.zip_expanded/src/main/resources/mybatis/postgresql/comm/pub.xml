<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kist.portal.common.pub.mapper.PublicMapper">

	<select id="selectAponJsonData" parameterType="sqlMap" resultType="sqlMap">
		/* selectAponJsonData */
		select aprv_no
		      ,aprv_usr_id
		      ,aprv_ttl
		      ,aprv_cntn
		      ,case when usg_yn = 'T' then 'Y' 
		            when usg_yn = 'Y' then 'Y'
		            else 'N' end as usg_yn
		from tb_co_aprv
		where aprv_usr_id = #{usrId}
		  and aprv_no = #{aprvNo}
	</select>
	
	<select id="selectAppVersion" parameterType="sqlMap" resultType="string">
		/* selectAppVersion */
		select buf_1st_cntn 
		  from tb_co_cd tcc 
		 where cd_grp_no = 'CO00000036'
	</select>
	
</mapper>