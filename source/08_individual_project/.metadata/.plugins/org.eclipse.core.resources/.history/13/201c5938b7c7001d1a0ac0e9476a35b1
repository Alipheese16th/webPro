package com.ch.movie.service;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import com.ch.movie.dao.UserDao;

public class RegisterService implements Service {

	@Override
	public void execute(HttpServletRequest request, HttpServletResponse response) {
		String userId = request.getParameter("");
		String userPw = request.getParameter("");
		String userName = request.getParameter("");
		
		String birthStr = request.getParameter("");
//		if("".equals(birthStr)) {
//			
//		}
		
		String userGender = request.getParameter("");
		String userEmail = request.getParameter("");
		String userTell = request.getParameter("");
		
		UserDao user = new UserDao();
		
		System.out.println(userId);
		System.out.println(userPw);
		System.out.println(userName);
		System.out.println(birthStr);
		System.out.println(userGender);
		System.out.println(userEmail);
		System.out.println(userTell);

	}

}
